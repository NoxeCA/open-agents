import React from 'react';
// Import the existing SectionMarker verbatim and re-export it so the rest of
// the jsonrender layer doesn't reach into lib/documents/quote directly.
export { default as SectionMarker } from '@/lib/documents/quote/components/SectionMarker';
